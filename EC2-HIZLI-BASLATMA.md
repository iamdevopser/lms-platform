# 🚀 EC2'de Hızlı Başlatma

## Tek Komutla Başlatma

```bash
cd /home/ec2-user/lms-platform && git pull origin minimal-sqlite 2>/dev/null; chmod +x start-lms.sh 2>/dev/null; ./start-lms.sh
```

Detaylar için `start-lms.sh` script'ine bakın.
